package method.overloading.overriding;

public class Main {

	public static void main(String[] args) {
		/*
		 * MathOperations obj = new MathOperations(); System.out.println(obj.add(5,
		 * 10)); // Calls first method System.out.println(obj.add(5, 10, 15)); // Calls
		 * second method System.out.println(obj.add(5.5, 2.3)); // Calls third method
		 */

		// Polymorphism (Dynamic Method Dispatch)
		/*
		Shape s1 = new Circle(); // // Upcasting
		Shape s2 = new Rectangle();

		s1.draw();
		s2.draw();
		*/
		
		// Summary:
		/**
		 * Method Overloading: Same method name, different parameters (Compile-time
		 * polymorphism).
		 * 
		 * Method Overriding: Subclass redefines a method from its superclass (Runtime
		 * polymorphism).
		 * 
		 * Polymorphism: One interface, multiple implementations (Runtime behavior).
		 */

	}

}
