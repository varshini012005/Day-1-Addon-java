package collections.Framework;

public class Main {

	public static void main(String[] args) {
		
		CollectionArrayList al = new CollectionArrayList();
		al.checkArrayList();
		
		

		/**
		 * The Java Collections Framework provides a set of classes and interfaces for
		 * storing and managing groups of objects efficiently. It includes Lists, Sets,
		 * Queues, and Maps.
		 * 
		 * 1. Hierarchy of Java Collections Framework
Collection (Interface)
   ├── List (Ordered, allows duplicates) → ArrayList, LinkedList, Vector
   ├── Set (Unique elements, unordered) → HashSet, LinkedHashSet, TreeSet
   ├── Queue (FIFO, ordered) → PriorityQueue, LinkedList (as Queue)
Map (Key-Value pairs, no duplicate keys) → HashMap, LinkedHashMap, TreeMap, Hashtable
		 */
		
		
	}

}
